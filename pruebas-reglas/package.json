{
  "name": "pruebas-reglas",
  "private": true,
  "description": "Pruebas automáticas de firestore.rules contra el emulador local (no toca producción).",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-transporte-perez \"node --test reglas.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0"
  }
}
